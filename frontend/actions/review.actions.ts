"use server";

/** Review mutations — wired when FEATURE_REVIEWS_ENABLED */
export async function placeholderReviewAction(): Promise<void> {
  // Intentionally empty until review Server Actions are implemented
}
